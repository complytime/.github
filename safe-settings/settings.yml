# Organization-wide repository settings for the complytime GitHub org.
# Applied to all managed repos. Suborg and repo-level overrides can
# extend but not weaken these defaults (enforced by override validators).
#
# IMPORTANT: This file must NOT set fields owned by peribolos:
#   description, has_projects, default_branch
# See MAINTAINING.md for the full tool boundary.
#
# MIGRATION NOTE: Existing repo-level rulesets (created manually via the
# GitHub UI) are being replaced by safe-settings managed rulesets.
# GitHub evaluates all active rulesets and the most restrictive rule
# wins, so both can coexist during migration. After verifying the
# safe-settings rulesets work correctly, the old repo-level rulesets
# should be deleted manually.
#
# Remaining repo-level rulesets to clean up:
#   complypack:  tags (active) — replaced by "release tag protection"
#   complytime:  main (active) — replaced by repos/complytime.yml
#
# Excluded from safe-settings management:
#   complyscribe:              archived
#   gemara-content-service:    archived (moved to complytime-labs)

repository:
  # Allow auto-merge on pull requests when all requirements are met.
  # Currently enabled on 7 repos; this will enable it on the remaining 4
  # (complytime, complytime-demos, website, complytime-policies).
  allow_auto_merge: true

  # Automatically delete head branches after pull requests are merged.
  # Currently only on complyctl; this enables it org-wide.
  delete_branch_on_merge: true

  # Merge strategies.
  # Squash and merge commit are enabled. Rebase merge is disabled
  # because it rewrites commit SHAs (breaking Signed-off-by
  # verification) and does not create a merge commit linking the
  # PR to the branch history, making git log and git bisect
  # harder to navigate. See STYLE_GUIDE.md § Pull Requests.
  allow_squash_merge: true
  allow_merge_commit: true
  allow_rebase_merge: false

  # Allow the "Update branch" button on PRs.
  # Currently only on complyctl; this enables it org-wide.
  allow_update_branch: true

  # Disable wiki org-wide. Documentation lives in dedicated repos.
  # Currently enabled on all repos; this is an intentional hardening.
  has_wiki: false

  # Dependabot vulnerability alerts and automated security fixes.
  # Currently only complyctl and org-infra have dependabot_security_updates
  # enabled; this enables it org-wide.
  security:
    enableVulnerabilityAlerts: true
    enableAutomatedSecurityFixes: true

# Rulesets applied at the org level.
# These create GitHub rulesets visible under Settings > Rules > Rulesets.
#
# NOTE on required_signatures: Not currently enforced on any repo.
# Adding it requires all contributors to configure GPG/SSH signing.
# Omitted from initial config. Add as a follow-up after contributor
# onboarding.
rulesets:
  # Ruleset for code repositories — strict branch protection.
  # Baseline derived from the complyctl and org-infra rulesets, which
  # are the most mature in the org.
  - name: "safe-settings: code repos"
    target: branch
    enforcement: active

    conditions:
      ref_name:
        include:
          - "~DEFAULT_BRANCH"
        exclude: []
      repository_name:
        include:
          - complyapi
          - complyctl
          - complypack
          - complytime-collector-components
          - complytime-policies
          - complytime-providers
          - homebrew-tap
          - org-infra
        exclude: []

    rules:
      # Restrict creation of refs matching the default branch pattern.
      # Prevents recreating the default branch if deleted.
      - type: creation

      # Prevent deletion of the default branch.
      - type: deletion

      # Prevent force pushes to the default branch.
      - type: non_fast_forward

      # Pull request requirements.
      # Baseline: 1 approver, dismiss stale reviews, code owner review,
      # last push approval. This matches the org-infra ruleset.
      #
      # complyctl currently requires 2 approvers — preserved via a
      # repo-level override in repos/complyctl.yml.
      #
      # required_review_thread_resolution is false everywhere today.
      # Kept false to match current state. Can tighten later.
      - type: pull_request
        parameters:
          required_approving_review_count: 1
          dismiss_stale_reviews_on_push: true
          require_code_owner_review: true
          require_last_push_approval: true
          required_review_thread_resolution: false

      # Preserve any status checks configured outside of safe-settings
      # (e.g., via individual repo workflow configurations).
      # Each repo has different CI checks (unit-test, test, test(3.10),
      # etc.) — EXTERNALLY_DEFINED retains existing checks on update.
      # On initial ruleset creation this rule is dropped (by design)
      # since there are no existing checks to retain. Once the ruleset
      # exists and checks are added externally, subsequent syncs
      # preserve them.
      - type: required_status_checks
        parameters:
          strict_required_status_checks_policy: true
          required_status_checks:
            - context: "{{EXTERNALLY_DEFINED}}"

  # Ruleset for non-code repositories — lighter protection.
  # Baseline derived from community, complytime-demos, and website
  # rulesets.
  - name: "safe-settings: non-code repos"
    target: branch
    enforcement: active

    conditions:
      ref_name:
        include:
          - "~DEFAULT_BRANCH"
        exclude: []
      repository_name:
        include:
          - community
          - complytime
          - complytime-demos
          - website
        exclude: []

    rules:
      # Restrict creation of refs matching the default branch pattern.
      - type: creation

      # Prevent deletion of the default branch.
      - type: deletion

      # Prevent force pushes to the default branch.
      - type: non_fast_forward

      # Pull request requirements — lighter than code repos.
      # 1 approver, code owner review required, no dismiss stale
      # reviews, no last push approval.
      # All parameters must be specified — the GitHub API rejects
      # partial pull_request parameter sets with HTTP 422.
      - type: pull_request
        parameters:
          required_approving_review_count: 1
          dismiss_stale_reviews_on_push: false
          require_code_owner_review: true
          require_last_push_approval: false
          required_review_thread_resolution: false

  # Ruleset for tag protection — prevent deletion or modification
  # of release tags. Protects supply chain integrity by ensuring
  # published version tags remain immutable. Replaces the manual
  # "tags" ruleset on complypack (id=18348763).
  - name: "safe-settings: release tag protection"
    target: tag
    enforcement: active

    conditions:
      ref_name:
        include:
          - "refs/tags/v*"
        exclude: []
      repository_name:
        include:
          - complyctl
          - complypack
          - complytime-collector-components
          - complytime-policies
          - complytime-providers
          - homebrew-tap
          - org-infra
        exclude: []

    rules:
      # Prevent deletion of release tags.
      - type: deletion

      # Prevent force-pushing release tags to a different commit.
      - type: non_fast_forward
